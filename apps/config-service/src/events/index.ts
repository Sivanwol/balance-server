import { SharedEvents } from '@wolberg-pro-games/utils-server/events';

export class LoadEvents extends SharedEvents {
  constructor() {
    super();
  }

  // @ts-ignore
  protected loadEvents() {
  }
}

